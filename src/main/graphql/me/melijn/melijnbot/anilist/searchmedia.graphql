query SearchMedia($name: String!, $type: MediaType!) {
    Page(page: 1, perPage: 10) {
        media(search: $name, type: $type) {
            id
            title {
                romaji
                english
                native
                userPreferred
            }
            siteUrl
            favourites
        }
    }
}